# Firebase Configuration
# 1. Copy this file to .env (cp .env.example .env)
# 2. Go to Firebase Console (https://console.firebase.google.com/)
# 3. Select your project > Project Settings > General tab
# 4. Scroll down to "Your apps" and click the web app config icon
# 5. Copy the config values below

VITE_FIREBASE_API_KEY=your-api-key-from-firebase-console
VITE_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your-sender-id-number
VITE_FIREBASE_APP_ID=1:your-sender-id:web:your-app-id

# Optional: Use Firestore emulator for development
# Set to "true" if you want to use the Firebase emulator
VITE_USE_FIRESTORE_EMULATOR=false

# Note: Don't commit the actual .env file with real values to version control
